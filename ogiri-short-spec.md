# 大喜利ショート動画 自動生成システム 仕様書

> Claude Code に渡す実装依頼書。Remotion をベースに、JSON を差し替えるだけで「笑える」縦型ショートを量産できる番組テンプレートを作る。

---

## 0. ゴール

- **尺**: 50〜60秒（目標58秒）の縦型ショート（1080×1920 / 30fps）
- **形式**: 1本＝1つのお題。お題に対して複数の回答を高速で繰り出す大喜利番組
- **思想**: 「センスのいい1発」だけに頼らない。複数の笑いの構造（テクニック）を1本の中に意図的に配置し、最後まで見させて笑わせる
- **量産性**: ネタは外部 JSON で管理。`npx remotion render` で MP4 出力。声・SE・BGM は素材ディレクトリ参照
- **ターゲット声**: 実録音（人間の声）を前提に設計しつつ、未収録時は無音＋テロップ＋SEでも成立する

---

## 1. 使用する「笑いのテクニック」と実装要件

仕様の核心。以下のテクニックを **データ構造とアニメーションの両方** で表現できるようにする。各テクニックはネタ JSON 側で `technique` フィールド等により指定し、テンプレが対応する演出を自動適用する。

### 1-1. 高速スライド（連発）
- 回答を 0.6〜1.2秒間隔で次々表示し、テンポで畳みかける
- 後半になるほど間隔を詰める「加速」を入れられること（`accelerate: true`）
- 各回答はスライドイン or ポップイン。同方向の単調反復を避け、左右交互・拡大などをローテーション

### 1-2. 伏線回収（フリと回収）
- 序盤の回答（フリ）で出した固有名詞・キーワードを、終盤の回答（オチ）で再利用して回収する
- JSON で `setup_id` を持つ回答と、それを参照する `callback_ref` を持つ回答を紐付け
- 回収時は、フリで使った要素を画面端に小さく再出現させる「想起演出」（フリのテロップが薄く再表示→オチで強調）を入れる
- 回収の瞬間に専用SE（`callback` SE）とフラッシュ

### 1-3. あるある
- 共感系の回答。視聴者が「わかる」と頷くタイプ
- 演出は派手にせず、テロップを素直に見せる（盛りすぎると共感が薄れる）
- `tag: "aruaru"` を付与。共感を促すため表示尺をやや長め（1.2〜1.5秒）に

### 1-4. スベってからの笑い（メタ・自虐）
- わざと弱い回答を置き、その直後に「（スベり）」演出を入れて、滑ったこと自体を笑いに変える
- 演出: 弱い回答の後にコオロギSE＋画面が一瞬暗転 or 砂嵐、テロップ「…。」→ツッコミテロップ（例「今のは無し」「real talk スベった」）
- JSON: `tag: "suberi"` ＋ 直後に `tag: "recovery"` のツッコミ回答をペアで配置
- これは1本に1回まで（多用するとただ寒くなる）

### 1-5. 天丼（同じボケの反復で笑わせる）
- 同じ型の回答を間隔をあけて2〜3回繰り返し、3回目で爆発させる
- `tag: "tendon"` ＋ `tendon_group` で同一グループを束ねる
- 反復ごとにテロップサイズ・SE音量を段階的に上げる

### 1-6. ピーク（本命オチ）
- 1本に必ず1つ、最も面白い本命回答を `peak: true` で指定
- 演出最大: ズームイン＋フラッシュ＋ドラムロール→ドン、テロップ最大サイズ、画面を一瞬止める「タメ」

---

## 2. 動画の構成（タイムライン）

| セクション | 目安尺 | 内容 | 狙い |
|---|---|---|---|
| フック | 0.0〜1.5s | お題を即提示（前置きゼロ）。「こんな○○は嫌だ」等 | スワイプ離脱を防ぐ |
| お題定着 | 1.5〜3.0s | お題を画面上部に固定表示（以降ずっと残す） | 文脈を保持 |
| 連発前半 | 3〜25s | 通常回答＋あるある＋伏線フリを配置。徐々に加速 | テンポで掴む |
| 中だるみ対策 | 25〜35s | スベり→回収、または天丼の1・2回目 | 緩急をつける |
| 連発後半 | 35〜50s | 加速MAX。伏線回収・天丼3回目 | 畳みかけ |
| ピーク | 50〜56s | 本命オチを最大演出 | 最大の笑い |
| 締め | 56〜58s | 一言＋次回フリ or チャンネル誘導 | 回遊 |

- お題は固定表示し続ける（上部に常駐）。回答エリアは中央〜下部
- セクション境界は実装上のガイド。実際の秒数は回答数とSEに応じて自動計算

---

## 3. データ構造（ネタ JSON）

`data/episodes/<id>.json` に1本分を格納。

```json
{
  "id": "ep001",
  "odai": "こんな結婚式は嫌だ。どんな結婚式？",
  "theme": "default",
  "answers": [
    {
      "id": "a1",
      "text": "受付がメルカリ",
      "tag": "normal",
      "duration": 1.0,
      "se": "pop"
    },
    {
      "id": "a2",
      "text": "新郎の友人代表スピーチが5部構成",
      "tag": "aruaru",
      "duration": 1.4,
      "se": "pop"
    },
    {
      "id": "a3",
      "text": "ご祝儀がPayPay（しかも送金エラー）",
      "tag": "normal",
      "setup_id": "paypay",
      "duration": 1.2,
      "se": "pop"
    },
    {
      "id": "a4",
      "text": "鳩を飛ばすはずが出てこない",
      "tag": "suberi",
      "duration": 1.2,
      "se": "weak"
    },
    {
      "id": "a5",
      "text": "（しーん）…今の鳩、楽屋で寝てた",
      "tag": "recovery",
      "duration": 1.6,
      "se": "cricket"
    },
    {
      "id": "a6",
      "text": "二次会の会場、さっきのPayPayでは入れない",
      "tag": "normal",
      "callback_ref": "paypay",
      "duration": 1.6,
      "se": "callback"
    },
    {
      "id": "a7",
      "text": "ブーケトスの中身が香典返し",
      "tag": "peak",
      "peak": true,
      "duration": 2.2,
      "se": "drumroll_don"
    }
  ],
  "outro": "あなたなら、どんな結婚式が嫌？コメントで大喜利して",
  "bgm": "uptempo_01",
  "voice_dir": "data/episodes/ep001/voice"
}
```

### フィールド定義

| フィールド | 必須 | 説明 |
|---|---|---|
| `id` | ○ | エピソードID |
| `odai` | ○ | お題テキスト（常時上部表示） |
| `theme` | | カラーテーマ名（`default` / `night` / `gameshow` 等） |
| `answers[]` | ○ | 回答配列。**表示順＝配列順** |
| `answers[].text` | ○ | 回答テロップ。改行は `\n` 可 |
| `answers[].tag` | ○ | `normal` / `aruaru` / `suberi` / `recovery` / `tendon` / `peak` |
| `answers[].peak` | | `true` の回答は本命演出（1本に1つ推奨） |
| `answers[].duration` | | 表示秒数。省略時はタグ既定値 |
| `answers[].se` | | SE名（`pop` / `weak` / `cricket` / `callback` / `drumroll_don` 等） |
| `answers[].setup_id` | | 伏線のフリ。任意のキー文字列 |
| `answers[].callback_ref` | | 回収する `setup_id` を参照。想起演出が発火 |
| `answers[].tendon_group` | | 天丼グループ名。同名でまとめて段階的に強調 |
| `outro` | | 締めの一言 |
| `bgm` | | BGM素材名 |
| `voice_dir` | | 各回答の音声ファイル置き場（`a1.wav` 等。あれば自動同期） |

---

## 4. 音声同期ルール

- `voice_dir` に `<answer.id>.wav`（または mp3）があれば、その回答の `duration` を **音声の実尺＋余白0.2s** に自動上書きする
- 音声が無い回答は JSON の `duration` を使用（無声でもテロップ＋SEで成立）
- お題ナレーション `odai.wav` があればフック区間に再生
- 実装: ビルド前スクリプト（`scripts/measure-audio.ts`）で各音声長を計測し、`duration` を解決した中間 JSON を生成 → Remotion に渡す

---

## 5. ビジュアル / 演出仕様

### 5-1. レイアウト
- 上部 0〜18%: お題バー（常時固定、半透明背景）
- 中央 25〜70%: 回答テロップ表示エリア
- 下部 85〜100%: コーナー名・カウンター（「回答 3/7」等、任意でON/OFF）
- セーフエリア: 左右 80px、上下 220px（UIアイコン回避）

### 5-2. テロップ
- フォント: 太め日本語ゴシック（例: Noto Sans JP Black / LINE Seed JP）。縁取り＋影で可読性確保
- 通常: 64〜80px。ピーク: 110〜130px
- 1行が長い場合は自動で2行に折り返し、はみ出す場合は自動縮小（fit-to-box）

### 5-3. タグ別アニメーション

| tag | 入り | 強調 | SE既定 |
|---|---|---|---|
| normal | ポップイン（scale 0.7→1, 0.15s） | なし | pop |
| aruaru | フェード＋軽い上げ | 表示長め | pop |
| suberi | 普通に出す | 直後に暗転/砂嵐0.3s | weak |
| recovery | 下からスッ | コオロギ＋小さめ | cricket |
| tendon | 毎回同じ動き | 回数ごとに拡大・音量UP | pop→don |
| peak | ズームイン＋静止 | フラッシュ＋ドラムロール→ドン | drumroll_don |

### 5-4. 伏線回収の想起演出
- `callback_ref` 発火時: フリで使ったテロップを画面端に **薄く0.4sだけ再表示** → メイン回答が出る瞬間にフリ要素を強調色でハイライト → `callback` SE＋リング状フラッシュ

### 5-5. 加速制御
- `accelerate: true`（エピソードルート or 後半区間）の場合、回答間の待ち時間を後半ほど線形に短縮（例: 1.0s → 0.5s）
- ピークの直前だけは必ず0.4sの「タメ（無音＋静止）」を入れて落差を作る

---

## 6. 技術スタック / プロジェクト構成

```
ogiri-shorts/
├─ package.json
├─ remotion.config.ts
├─ src/
│  ├─ Root.tsx                # Composition登録（1080x1920, 30fps）
│  ├─ OgiriShort.tsx          # メインComposition
│  ├─ sections/
│  │  ├─ Hook.tsx             # フック（お題即出し）
│  │  ├─ OdaiBar.tsx          # お題常駐バー
│  │  ├─ AnswerSequence.tsx   # 回答連発の制御（尺計算・加速）
│  │  └─ Outro.tsx            # 締め
│  ├─ answers/
│  │  ├─ AnswerCard.tsx       # tag分岐でアニメ切替
│  │  ├─ Callback.tsx         # 伏線回収の想起演出
│  │  └─ Suberi.tsx           # スベり→回収演出
│  ├─ fx/
│  │  ├─ Flash.tsx, Shake.tsx, Zoom.tsx
│  │  └─ useSE.ts             # SE再生フック
│  ├─ theme/themes.ts         # カラーテーマ定義
│  └─ lib/
│     ├─ timeline.ts          # 回答配列→フレーム割当（加速・タメ計算）
│     └─ types.ts             # JSON型定義
├─ scripts/
│  ├─ measure-audio.ts        # 音声長計測→duration解決
│  └─ render-batch.ts         # data/episodes/*.json を一括レンダリング
├─ data/
│  ├─ episodes/*.json
│  └─ episodes/<id>/voice/*.wav
└─ assets/
   ├─ se/  (pop, weak, cricket, callback, drumroll_don, ...)
   ├─ bgm/
   └─ fonts/
```

- **Remotion**（React/TypeScript）。Composition は 1080×1920・30fps・尺は timeline.ts が動的算出
- SE/BGM は `<Audio>` で配置。BGMはループ＋ピークで一瞬ダッキング（音量を下げる）
- 出力: `out/<id>.mp4`（H.264, yuv420p, 縦）

---

## 7. レンダリングフロー

```bash
# 1. 音声長を計測してdurationを解決（音声がある場合）
npx ts-node scripts/measure-audio.ts data/episodes/ep001.json

# 2. 単体プレビュー
npx remotion studio

# 3. 単体レンダリング
npx remotion render OgiriShort out/ep001.mp4 --props=data/episodes/ep001.resolved.json

# 4. 一括レンダリング（data/episodes配下の全JSON）
npx ts-node scripts/render-batch.ts
```

- 仕上げ（最終のサムネ用フレーム抜き・微調整）は CapCut で行う前提。MP4はそのまま投稿可能な品質で出す

---

## 8. timeline.ts のロジック要件（重要）

回答配列からフレーム割当を計算するコア。以下を満たすこと:

1. 各回答の `duration`（音声解決後）を秒→フレームに変換し、開始フレームを累積
2. `accelerate` 区間は回答間ギャップを後半ほど短縮（最小0.4s）
3. `peak: true` の直前に固定0.4sの「タメ」フレームを挿入
4. `suberi` の直後は必ず `recovery` が来る前提で、暗転0.3sを挟む
5. `callback_ref` がある回答は、開始の0.4s前から想起演出レイヤーを重ねる
6. `tendon_group` の各回答に出現順インデックス（1,2,3…）を付与し、強調段階を決定
7. 合計尺が **60sを超える場合は警告**を出し、`duration` の自動圧縮 or 回答削減を提案（自動では削らない）
8. 合計尺が **45s未満の場合も警告**（薄い動画になるため回答追加を提案）

---

## 9. ネタ生成ルール（Claude が回答案を作るときの指針）

> ネタ JSON の中身（回答テキスト）を Claude に量産させる際の品質基準。**最終選別は人間が行う前提**で、候補を多めに出す。

- お題1つにつき回答候補を **15〜20個** 出す（採用は7前後）
- 1本の中に必ず含める: あるある×1〜2、伏線フリ＆回収のペア×1、ピーク×1
- 任意で含める: スベり→回収×最大1、天丼×最大1
- フリと回収は **同じ固有名詞/概念** を使い、回収側は「まさかそう繋がるか」の意外性を持たせる
- 回答は短く（原則20文字以内、長くて2行）。読み切れない長文は不可
- 下品・誹謗中傷・特定個人攻撃・差別ネタは不可。あくまで状況の可笑しさで笑わせる
- 同じ笑いの型を連続させない（normalとaruaruを交互に置くなど緩急をつける）

---

## 10. 受け入れ基準（Definition of Done）

- [ ] `data/episodes/<id>.json` を差し替えるだけで別エピソードがレンダリングできる
- [ ] 6つのテクニック（高速スライド/伏線回収/あるある/スベり/天丼/ピーク）が全てJSON指定で発火する
- [ ] 音声ファイルがあれば自動で尺同期、無ければJSONのdurationで成立する
- [ ] お題が最後まで上部に固定表示される
- [ ] ピーク回答で明確に演出が最大化される（ズーム＋フラッシュ＋SE）
- [ ] 出力が 1080×1920 / 30fps / H.264 の縦MP4で、尺が45〜60sに収まる
- [ ] `render-batch.ts` で複数本を一括出力できる
- [ ] サンプルエピソード（ep001）が同梱され、そのままレンダリングして笑える1本になる

---

## 11. 最初に作ってほしいもの（マイルストーン）

1. **M1**: Remotionプロジェクト雛形＋ `OgiriShort` Composition、お題バー常駐、normal回答の高速スライドだけ動く最小版＋ep001サンプル
2. **M2**: タグ別アニメ（aruaru/peak）とSE再生、timeline.tsの加速・タメ
3. **M3**: 伏線回収（想起演出）とスベり→回収、天丼
4. **M4**: 音声同期スクリプトと一括レンダリング

各マイルストーンごとにレンダリング結果を確認できる状態で区切ること。
